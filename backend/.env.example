# Capital Cipher AI — Phase 1 (PAPER mode only)
# No private exchange API keys are allowed in Phase 1 (docs/16-security-rules.md).
APP_ENV=local
SYSTEM_MODE=PAPER
DATABASE_URL=sqlite+aiosqlite:///./capital_cipher.db
LOG_LEVEL=INFO
ALLOWED_SYMBOLS=BTCUSDT,ETHUSDT,SOLUSDT
DEFAULT_TIMEFRAME=15m
DEFAULT_EXCHANGE=BINANCE
ENABLE_MARKET_DATA=0

# Auth for sensitive endpoints (kill switch). Generate your own value.
ADMIN_API_KEY=

# Risk limits (docs/06)
RISK_PER_TRADE_PERCENT=1.0
MAX_DAILY_DRAWDOWN_PERCENT=5.0
MAX_CONSECUTIVE_LOSSES=3
MAX_OPEN_POSITIONS=3

# Paper trading (docs/18)
PAPER_INITIAL_BALANCE=10000
FEE_RATE_PERCENT=0.08
SLIPPAGE_RATE_PERCENT=0.02

# Decision engine (docs/25)
MINIMUM_CANDIDATE_CONFIDENCE=70
