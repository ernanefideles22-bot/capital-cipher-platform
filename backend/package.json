{
  "name": "@capital-cipher/backend",
  "version": "0.17.0",
  "private": true,
  "scripts": {
    "build": "python -m compileall -q app",
    "test": "python -m pytest -q -p no:cacheprovider"
  }
}
